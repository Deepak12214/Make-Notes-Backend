<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script> 
    <title>Document</title>
</head>
<body>
    <div class="main text-white w-full min-h-screen bg-zinc-900">
        <div class="heading  px-10  pt-6">
            <h1 class="text-3xl text-blue-500">
                Makes Notes...
            </h1>
        </div>
        <div class="form px-10 pt-4 text-white">
            <form action="/create" method="post">
                <input type="text" class="w-full rounded-md outline-none mt-2 px-5 py-2 bg-zinc-700 " placeholder="Title goes here" name="title" >
                <textarea placeholder="Write your task details here..."  name="detail" id="" class="w-full rounded-md outline-none mt-2 px-5 py-2 bg-zinc-700 resize-none"></textarea>
                <input type="submit" class="nt-2 bg-blue-600 px-5 py-2 rounded-md">
            </form>
        </div>
        <div class="task flex gap-3 flex-wrap p-10 text-white">
            <% if(files.length>0){ %>
               <% files.forEach((file)=>{ %>
               <div class="task w-72 px-3 py-4 rounded-md  bg-zinc-700"> 
                <h1 class="text-2xl tracking-tighter"><%=file%></h1> 
             <div class="flex mt-3 justify-between ">
                <a class="text-blue-500 inline-block  " href='/file/<%=file%>'>Read more..</a> 
                <a class="text-zinc-500 inline-block " href='/edit/<%=file%>'>edit filename</a> 
             </div>
            </div> 
             <%  }) %>
           <% }  else { %>
            <h3 class="text-zinc-700">no tasks yet</h3>
            <%}%>
        </div>
    </div>
</body>
</html>